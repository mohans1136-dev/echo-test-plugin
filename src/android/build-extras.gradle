repositories {
    google()
    mavenCentral()
}

ext.postBuildExtras = {
    android {
        defaultConfig {
            minSdkVersion 28
        }
        sourceSets {
            main {
                java.srcDirs += 'src/main/java'
                kotlin.srcDirs += 'src/main/java'
            }
        }
    }
}

dependencies {
    implementation 'com.clerk:clerk-android-api:1.0.35'
    implementation 'org.jetbrains.kotlinx:kotlinx-coroutines-android:1.7.3'
}
